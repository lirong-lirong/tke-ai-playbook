{{- if .Values.pd.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm.decodeName" . }}-service
  labels:
    {{- include "helm.labels" . | nindent 4 }}
    component: decode
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.decode.service.port }}
      targetPort: {{ .Values.decode.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "helm.selectorLabels" . | nindent 4 }}
    component: decode
{{- end }}